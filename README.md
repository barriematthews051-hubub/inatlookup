
Given an iNaturalist photo URL, recover the parent observation by using a locally-built index of the iNaturalist Open Data photos table.

## Why this exists

iNaturalist photo URLs contain a photo ID, but not directly the observation ID.

inatlookup bridges that gap by using a local index of the iNaturalist Open Data photos table.